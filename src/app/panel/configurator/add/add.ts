import {Component} from '@angular/core';

const style = require('./add.css');
const template = require('./add.html');

@Component({
  selector: 'add',
  template,
  styles: [style]
})

export class Add {
  constructor() {
  }

}
